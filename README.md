# EcoEnergy

## Descripción y objetivo

**EcoEnergy** es un proyecto Back End desarrollado utilizando **Python** y **Django**.

El objetivo inicial del proyecto es aplicar los conceptos y herramientas utilizados en el desarrollo Back End con Django.

Ruta de trabajo:

```bash
~/Documents/Django_1
```

## Requisitos previos

Antes de ejecutar el proyecto, es necesario contar con:

* Python
* pip
* Git
* Un entorno virtual de Python

## Clonación del repositorio

Clonar el repositorio:

```bash
git clone <URL_DEL_REPOSITORIO>
```

Acceder al directorio del proyecto:

```bash
cd ~/Documents/Django_1
```

> La URL del repositorio debe reemplazarse cuando esté disponible.

## Creación y activación del entorno virtual

Crear el entorno virtual `.venv`:

```bash
python -m venv .venv
```

Activar el entorno virtual:

### Linux / macOS / Git Bash

```bash
source .venv/bin/activate
```

### Windows CMD

```bash
.venv\Scripts\activate
```

### Windows PowerShell

```powershell
.venv\Scripts\Activate.ps1
```

## Instalación de dependencias

Con el entorno virtual activado, instalar las dependencias desde `requirements.txt`:

```bash
pip install -r requirements.txt
```

## Comandos de verificación

Verificar que Python está instalado:

```bash
python --version
```

Verificar pip:

```bash
pip --version
```

Verificar Django:

```bash
python -m django --version
```

Verificar las dependencias instaladas:

```bash
pip list
```

## Estado actual

🚧 **Proyecto en desarrollo.**

Actualmente se encuentra en su etapa inicial de configuración.

## Próximos pasos

* Configurar el entorno de desarrollo.
* Instalar las dependencias del proyecto.
* Configurar Django.
* Definir la estructura inicial del proyecto.
* Actualizar este README a medida que avance el desarrollo.
